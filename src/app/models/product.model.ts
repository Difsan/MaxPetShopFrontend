export interface Product{
    idDTO: string,
    nameDTO: string,
    brandDTO: string,
    descriptionDTO: string,
    imageDTO: string,
    animalTypeDTO: string,
    categoryDTO: string,
    unitaryPriceDTO: number,
    inventoryDTO: number,
    inStockDTO: boolean 
}